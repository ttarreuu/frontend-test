// Write the function to check if all open brackets are closed in order

function bracketCheck(str) {
  // return true if all open brackets are closed in order
}

// it("bracket checks {[](())}", expect(bracketCheck("{[](())}")).toBe(true));
// it("bracket checks {[((])}", expect(bracketCheck("{[((])}")).toBe(false));
// it("bracket checks {[](()}", expect(bracketCheck("{[](()}")).toBe(false));
// it("bracket checks {}", expect(bracketCheck("{}")).toBe(true));
